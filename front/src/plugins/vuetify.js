import Vue from 'vue';
import Vuetify, {VApp, VListItem, VListItemAction, VListItemContent, VTextField} from 'vuetify/lib';

Vue.use(Vuetify,{ VApp, VListItem, VListItemAction, VListItemContent, VTextField});

export default new Vuetify({
});
